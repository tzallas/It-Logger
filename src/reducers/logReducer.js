const initialState = {
    logs: null,
    current: null,
    loading: false,
    error: false
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
